apiVersion: v1
kind: Service
metadata:
  name: {{ include "maximo-mcp.fullname" . }}
spec:
  selector:
    app: {{ include "maximo-mcp.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
